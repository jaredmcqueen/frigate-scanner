# Frontend — dashboard (stub)

Placeholder for the dashboard UI. Planned: a lightweight Vite app that reads scan
results from the backend (FastAPI over `frigate.db`) and renders the open-instance
view that `check_open.py` currently bakes into static HTML.

Not scaffolded yet — see the TODO in [`../backend/README.md`](../backend/README.md).
